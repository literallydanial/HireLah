# Secrets — contains Anthropic API key and SMTP/Gmail app password.
# Keep this file on the server, just don't track it in git.
config.json

# OS / editor junk
.DS_Store
Thumbs.db

# Logs
*.log
error_log

# Local backup copies
*.backup
